app-id: "dev.eden-emu.Eden"
runtime: "org.freedesktop.Platform"
runtime-version: "25.08"
sdk: "org.freedesktop.Sdk"

command: "eden"

finish-args:
  - "--device=all"
  - "--allow=bluetooth"
  - "--share=network"
  - "--share=ipc"
  - "--socket=pulseaudio"
  - "--talk-name=org.freedesktop.ScreenSaver"
  - "--socket=wayland"
  - "--socket=fallback-x11"

modules:
  - name: eden
    buildsystem: simple
    sources:
      - type: file
        path: Eden-x64.AppImage
    build-options:
      strip: false
      no-debuginfo: true
    build-commands:
      - "chmod +x Eden-x64.AppImage"
      - "./Eden-x64.AppImage --appimage-extract"
      - "mv squashfs-root/usr/* \"${FLATPAK_DEST}/\""
      - "rm -fr squashfs-root"